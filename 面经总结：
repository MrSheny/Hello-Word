一 . mysql 的一些问题：
    1.1 mysql 的优化 和 基本操做：
        优化： 1） sql 语句的优化 和代码的优化。
               2） mysql 数据库本身的优化。
        sql 基本语句：
        
        索引语句：

    1.2myisam 和 innodb 的区别（有空买本书看看）：

         答： 1.innodb 行锁，怎么使用，失效条件，等等。(创建索引语句怎么创建（sql单列，组合，唯一，全文，bimap等等）)

              2.innodb 对事务的支持，使用mvcc来实现的。默认隔离级别为 可重复读，其实他只是用mvcc 的思想来解决并发读的问题，快照读的时候不需要加锁，
                在对最新数据进行读，进行更新的时候还是要加锁的（读写锁），因为
                innodb 没法解决 多个更新一起提交的问题（举例说明）。 另外为了解决幻影读的问题，使用mvcc + ..key 来就解决（详细描述）.

              3. 索引使用的数据结构的不同（详解） 主键，唯一，多列，全文索引。全文索引的倒排索引（详解）。

            mvcc 不能解决幻读的问题：
              https://www.jianshu.com/p/cef49aeff36b
      


二. 关于redis 回答（买书看看，看别人博客不成体系）：

   答： 1.redis 的基本概念 基于内存数据库，键值对存储数据,是基于cs 的架构的 客户端，server 端，可支持的数据类型（5种每种数据类型的底层实现是怎么样的
          例如 sortedset 底层是怎么实现的（跳表） 和 java中 的 collection（sortedset的实现treeset有何不同）），对应的基本操做等等。
   
        2.亦可持久化，持久化的两种方式，RDB,AOF 详解，他们的相同点，和不同点，应用场景。
        
        3.并且海内置了 replication(复制) ， LRU驱动事件， 事务 ， 哨兵， 自动分区（cluster）。对上面每一个提到的知识点必须深入了解，讲出来。
          3.1哨兵：主要是用来解决，集群监控和故障转移。（主节点出现故障哨兵投票选举主master）
        
        4.自己在项目中怎么应用redis的 （eg: 解决sesion 不一致问题（详解），分布式锁实现（详解））
           4.1  主要是对应时效性要求比较高的数据的解决方案 数据库与缓存双写的方案（数据不一致问题的解决），
           4.2 对于时效性要求不高的数据怎么解决的？ 异步更新缓存结构的方案（三级缓存架构） ）
           
二.1 Nginx 的一些问题：
    

三. 关于java的一些思考：

    java知识点：（这一部分内容量真的很大好好学习）
        1.什么叫面向对象，你是怎么理解的，三大特性的理解（封装继承和多态）。
        2.位操做，访问权限控制（public,private,protected,包？），数组，字符串（正则表达），基本类型，泛型，枚举。
        3.容器，
        4.IO（磁盘），
        5.Socket（网络io）.（netty）
        6.并发。
        7.jvm(深入理解jvm)


四. spring，MVC,mybatis

    spring:
        1. spring 的启动流程，
        2.spring bean 的作用域，
        3.spring 注解实现原理
        4.beanFactory 和 FactoryBean的区别
        5.事务
        6. AOP 和 ioc 各自的原理，ioc实现原理没有无参构造函数可以实现注入吗？
        7.反射具体怎么实现的，方法区，然后类加载机制（双亲，双亲的破坏）。

五. tomcat的一些回答：

    tomacat 都有哪些功能。
    怎么调优。



六. 深入理解计算机系统 和计算机网络
   
   
   计算机网络：
   
       http 和 tcp 的问题：
       
              http 分为 请求（request） 和 响应（response）， 请求分为请求行，请求头，请求体，响应与之对应。
              请求头 和 请求行 里面分别包含什么内容。 响应头和响应行里面分别包含什么内容。
                 https://blog.csdn.net/holmofy/article/details/68492045
                 
              http  和 https区别：
                 https://www.cnblogs.com/wqhwe/p/5407468.html
                  
              
              tcp数据流切分按什么规则来切分的？
                     根据网络拥塞情况，和响应窗口值来确定数据包切分的大小,以及发送缓冲区生于大小。
                     
              tcp 和 udp 的区别， Tcp图。
                  tcp提供有连接的，可靠传输， 点对点通信， udp 不可靠无连接，只是简单的把接收到的数据加上 udp包头发送出去，不对数据进行任何处理，
                  由于 udp 不对数据进行处理只是简单的发送出去，所以消息有明显的界限，不会发生拆包粘包的问题。
            
              tcp 粘包/拆包问题：
                  由于 tcp 对消息进行了重新 的才分 和 组合（网络和链路cheng）
              
              拥塞控制：
                 拥塞控制是发送端 持有一个拥塞窗口 根据在固定一段时间内能不能接收到 服务端接收到消息的响应，来判断是不是发生了拥塞，
                 算法 慢开始算法（初值唯一每次变为原来的二倍）， 阀值，拥塞控制算法（线性）
              
              

  计算机系统：
  
  
  
   

七.数据结构与算法：
    
  剑指offer 和 leetcode
  
    剑指offer 题目分类：
      1.数组，链表，二叉树。
      
      2.动态规划，回溯和贪心。
    
    基本的排序查找算法：
    
    LRU 算法的实现（加一些限制条件）：
       可以使用Linkedhashmap 来实现 ， 复习java 中 hashmap treemap 和 linkedhashmap 的区别以及他们各自的作用。
    
    
    
    

八. 项目(docker，zookeeper，kafka,dubbo/spring-cloud)
